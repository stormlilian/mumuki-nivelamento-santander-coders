// variaveis E TIPOS DE DADOS
// 2.declaração de variaveis:
// declare duas variaveis, onde uma se chamará idade e outra que se chamará peso. Em seguida, execute a sua resposta.
// let idade
// let peso
// 3. armazenando dados
// Para passar para o próximo exercício, declare duas variaveis ​​(idade e peso) e atribua a elas um valor numérico. Em seguida, execute a sua resposta.

let idade = 26
let peso = 100

// 4 sempre falta ponto e virgula?
// Crie as variaveis meses e quantidadeDeAlunos
let meses = 12;
let quantidadeDeAlunos = 30;

// 5.variaveis e operações
// Vamos ver se está entendido: Declare e atribua duas variaveis, numeroA e numeroB, e então nas variaveis resultadoSoma,resultadoSubtracao, resultadoMultiplicacao eresultadoDivisao armazene os cálculos feitos utilizando numeroA e numeroB nas variaveis de resultado, de modo que o cálculo matemático se altere de acordo com o título da variavel, por exemplo. Exemplo let resultadoSoma = (numeroA + numeroB) Como nossa variavel resultadoSoma indica nós devemos fazer uma soma utilizando as variaveis numeroA e numeroB. Para testar a sua resolução, você precisa apenas executar a sua resposta.


// Exemplos:
// soma: a + b
// subtração: a - b
// multiplicação: a * b
// divisão: a / b

let numeroA = 2;
let numeroB = 3;
let resultadoSoma = numeroA + numeroB;
let resultadoSubtracao = numeroA - numeroB;
let resultadoMultiplicacao = numeroA * numeroB;
let resultadoDivisao = numeroA / numeroB;


// 6.outro tipo de dado
// Para passar para o próximo exercício, declare a variavel saudacao e atribua o texto "Hello World".  E finalmente imprima o valor da variavel na tela usando o `console.log`

let saudacao ="Hello World";
console.log(saudacao)

// 7.mais operacões
// Declare e atribuia duas variaveis, uma com o seu nome e outra com o seu sobrenome, e então na variavelnomeCompleto mantenha tudo junto.
// Exemplo: "Cesar Michelin"
// Observação: Não esqueça de concatenar um espaço entre as variaveis!

let nome = "Lilian"
let sobrenome = "Gomes"
let nomeCompleto = nome + " " + sobrenome

// 8.outras operações
// Declarar e atribuir três variaveis, uma com o seu 'nome', outra com o seu 'sobrenome' e outra com a sua 'idade'. Então, declare uma variavel com nome resultado e atribua uma string unindo suas variaveis da seguinte forma: " João Silva terá 30 anos" e utilize o console.log pra mostrar o que acontece. Respeite os espaços!

let nome = "Lilian"
let sobrenome = "Castro"
let idade = 26
let resultado = nome + " " + sobrenome + " " + "terá" + " " + idade + " " + "anos"
console.log(resultado)

// 9.trocar valores
// Vamos ver se entendeu: Defina a variavel numeroA com um valor de 30 e defina a variavel numeroB com um valor de 45. Em seguida, faça a variavel numeroA armazenar o valor da variavel numeroB e vice-versa.

let numeroA = 30
let numeroB = 45
let numeroC = numeroA
numeroA = numeroB
numeroB = numeroC

// 10. mais tipos de dados
// Para continuar, defina uma variavel gostoDeSorvete e atribua a ela um valor booleano. Então, utilize console.log(gostoDeSorvete) para ver o que acontece!

let gostoDeSorvete = true;
console.log(gostoDeSorvete)

// 11. mais sobre booleanos
// Para continuar, defina duas variaveis: umNumeroPequeno e umNumeroGrande, e atribua a elas valores numéricos diferentes de acordo com seus nomes. Então defina a variavel eMenor e atribua o resultado da comparação se umNumeroPequeno é menor do que umNumeroGrande; e defina a variavel eMaior, com o resultado da comparação umNumeroPequeno é maior que umNumeroGrande.

let umNumeroPequeno = 2
let umNumeroGrande = 11
let eMenor = umNumeroPequeno < umNumeroGrande
let eMaior = umNumeroPequeno > umNumeroGrande


